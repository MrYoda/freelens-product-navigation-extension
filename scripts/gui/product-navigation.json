{
  "clusters": [
    { "id": "cluster-a", "name": "Cluster A" },
    { "id": "cluster-b", "name": "Cluster B" },
    { "id": "cluster-missing", "name": "Missing Cluster" }
  ],
  "products": [
    { "id": "commerce", "name": "Commerce" }
  ],
  "services": [
    {
      "id": "checkout",
      "name": "Checkout",
      "productId": "commerce",
      "components": [
        { "id": "checkout-a", "name": "Checkout A", "targets": [{ "namespace": "team-a", "clusters": ["cluster-a"] }] },
        { "id": "checkout-b", "name": "Checkout B", "targets": [{ "namespace": "team-b", "clusters": ["cluster-b"] }] },
        { "id": "shared", "name": "Shared", "targets": [
          { "namespace": "shared", "clusters": ["cluster-a", "cluster-b"] },
          { "namespace": "team-a", "clusters": ["cluster-a"] }
        ] },
        { "id": "missing", "name": "Unavailable", "targets": [{ "namespace": "unavailable", "clusters": ["cluster-missing"] }] }
      ]
    }
  ],
  "hidden": { "services": {} }
}
